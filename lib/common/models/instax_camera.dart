class InstaxCamera {
  final int id;
  final bool isLimited;
  final String title;
  final double price;
  final String image;
  final int color;

  InstaxCamera({required this.id, required this.isLimited, required this.title, required this.price, required this.image, required this.color});
}